/**
 * Kotlin 특징
 * 1. 패키지 레벨 함수 : 자바와 달리 코틀린은 클래스에서 완전히 독립된 함수를 가지고 있을 수 있다.
 * 2. 데이터 타입을 맨 끝에 명시
 * 3. 매 문잔의 끝에 세미콜론을 적지 않는다. (세미콜론은 두 문장을 한 줄에 적을때만 사용)
 */
fun main(args : Array<String>) : Unit {
    println("Hello, Kotlin")
}